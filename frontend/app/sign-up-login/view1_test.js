'use strict';

describe('myApp.sign-up-login module', function() {

  beforeEach(module('myApp.sign-up-login'));

  describe('sign-up-login controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});